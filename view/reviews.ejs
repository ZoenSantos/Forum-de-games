<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GameGenius - Reviews</title>
    <link rel="icon" href="logo.png" type="image/x-icon">
    <link rel="stylesheet" href="style.css">
    <!-- Link para a fonte de jogos -->
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Press Start 2P', cursive;
            background-color: #f4f4f4;
            text-align: center;
        }
        header {
            background-color: #333;
            color: white;
            padding: 20px;
        }
        nav a {
            color: white;
            text-decoration: none;
            margin: 0 15px;
        }
        .auth-buttons a {
            margin-left: 10px;
            color: white;
            background-color: #4b6cb7;
            padding: 8px 12px;
            border-radius: 5px;
            text-decoration: none;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <header>
        <h1>Reviews de Jogos</h1>
        <nav>
            <a href="/">Inicio</a>
            <a href="/dicas">Dicas</a>
            <a href="/reviews">Reviews</a>
            <a href="/easter-eggs">Easter Eggs</a>
        </nav>
        <div class="auth-buttons">
            <a href="/login" class="login">Login</a>
            <a href="/cadastro" class="cadastro">Cadastro</a>
        </div>
    </header>
    <main>
        <h2>Leia as últimas reviews!</h2>
        <p>Aqui você encontra análises detalhadas sobre os jogos mais recentes.</p>
        <p>Confira também o vídeo com as melhores análises no <a href="https://www.youtube.com/watch?v=m2xswMyqgsw&list=PLaV1nMv3TVykN0Es6m23DA1X_3HgNKJLq&pp=iAQB" target="_blank">YouTube</a>.</p>
       <br>
        <div class="container">
            <h1>Postagens Recentes</h1>
            <ul id="postagens-list"></ul>
    
            <script>
                // Faz uma solicitação para a rota API que retorna as postagens
                fetch('/api/postagens')
                    .then(response => response.json())
                    .then(postagens => {
                        const postagensList = document.getElementById('postagens-list');
                        
                        if (postagens && postagens.length) {
                            postagens.forEach(post => {
                                const listItem = document.createElement('li');
                                listItem.innerHTML = `<a href="/lerReview/${post.id}">${post.titulo}</a>`;
                                postagensList.appendChild(listItem);
                            });
                        } else {
                            const listItem = document.createElement('li');
                            listItem.textContent = 'Nenhuma postagem disponível.';
                            postagensList.appendChild(listItem);
                        }
                    })
                    .catch(error => {
                        console.error('Erro ao obter postagens:', error);
                    });
            </script>
        </div>


    </main>
</body>
</html>
